#!/usr/bin/env node

console.log("🧪 Running strict lint checks and type checking before pushing...")
const { execSync } = require('child_process')

try {
  execSync('npm run lint:strict && npm run type-check', { stdio: 'inherit' })
} catch (error) {
  process.exit(1)
} 