// Test file without lint errors
import React from 'react';

const TestLintFixed = () => {
  // This is a clean component without lint errors
  const message = "This component has no lint errors";
  
  return <div>{message}</div>;
};

export default TestLintFixed; 